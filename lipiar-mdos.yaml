apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: limpiar-mdos
  namespace: mh-dev-dinafi-usi-cicloci
spec:
  tasks:
    - name: clean-mdos
      taskRef:
        params:
          - name: kind
            value: task
          - name: name
            value: clean-mdos
          - name: namespace
            value: openshift-pipelines
      workspaces:
        - name: maven-repo
          workspace: mdos
  workspaces:
    - name: mdos
      optional: false
  params: []
  finally: []

